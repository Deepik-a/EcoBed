<style>
    body {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        background-color: #1a1a1a;
        margin: 0;
        font-family: 'Arial', sans-serif;
        overflow: hidden;
    }

    .error-container {
        text-align: center;
        background-color: #232323;
        padding: 3em;
        border-radius: 10px;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        transform: scale(0);
        animation: scale-in 0.5s forwards ease-out;
    }

    @keyframes scale-in {
        to {
            transform: scale(1);
        }
    }

    .xmark-container {
        margin-bottom: 1em;
    }

    .xmark {
        width: 100px;
        height: 100px;
    }

    .xmark__circle {
        stroke: #f44336;
        stroke-width: 4;
        stroke-dasharray: 166;
        stroke-dashoffset: 166;
        stroke-linecap: round;
        animation: draw-circle 1s ease-out forwards;
    }

    .xmark__cross {
        stroke: #f44336;
        stroke-width: 4;
        stroke-dasharray: 48;
        stroke-dashoffset: 48;
        stroke-linecap: round;
        animation: draw-cross 1s 0.5s ease-out forwards;
    }

    @keyframes draw-circle {
        to {
            stroke-dashoffset: 0;
        }
    }

    @keyframes draw-cross {
        to {
            stroke-dashoffset: 0;
        }
    }

    .error-message {
        color: #f44336;
        margin-bottom: 0.5em;
        opacity: 0;
        animation: fade-in 1s 0.5s forwards;
    }

    .order-details {
        color: #fefeeb;
        margin-bottom: 1.5em;
        opacity: 0;
        animation: fade-in 1s 1s forwards;
    }

    @keyframes fade-in {
        to {
            opacity: 1;
        }
    }
</style>

<div class="error-container">
    <div class="xmark-container">
        <svg class="xmark" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 52 52">
            <circle class="xmark__circle" cx="26" cy="26" r="22" fill="none" />
            <path class="xmark__cross" fill="none" d="M16 16 36 36 M36 16 16 36" />
        </svg>
    </div>
    <h1 style="color: #Fefeeb;">Sorry.</h1>
    <h1 class="error-message">Order Failed!</h1>
    <p class="order-details">Unfortunately, your order could not be processed. Please try again later.</p>
    <p style="color: #555;">
        If you continue to experience issues, please contact our support team for assistance.
    </p>
    <div class="d-flex justify-content-center gap-5 mt-5">
        <a href="/orders" class="btn btn-danger d-flex">My Orders</a>
        <a href="/home" class="btn btn-danger d-flex">Continue Shopping</a>
    </div>
</div>
